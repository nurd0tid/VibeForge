# Daily Log ‚Äî 2026-06-25

---

## Session ‚Äî Runtime fixes and Document Studio

### Prompt

User reported the app was still serving on port 3000, theme hydration/script errors, project browser not opening folders clearly, and requested a flexible Word/PPT/Excel workspace that can start from a prompt or existing file, preview/edit directly in the web app, and hand off work to the AI kanban flow. Figma should be prepared as coming soon with a reminder modal.

### Plan

- Replace `next-themes` with a small local theme provider to avoid injected script and hydration mismatch.
- Fix JSON content-type handling for empty POST requests, especially NocoDB setup.
- Move the web UI default from port 3000 to 3456 across scripts, env, allowed origins, Playwright, and docs.
- Improve the local folder picker with roots, parent navigation, current folder selection, and Git indicators.
- Add Document Studio for Word/PPT/Excel from prompt or file, editable preview, export text, and kanban task creation.
- Add Figma coming-soon modal scaffold without enabling auth or design mutation yet.

### Changed Files

- `apps/web/lib/api.ts`
- `apps/orchestrator/src/nocodb.ts`
- `apps/web/components/providers.tsx`
- `apps/web/components/dashboard.tsx`
- `apps/web/components/dialogs.tsx`
- `apps/orchestrator/src/git.ts`
- `apps/orchestrator/src/config.ts`
- `apps/web/components/document-studio.tsx`
- `apps/web/components/figma-coming-soon.tsx`
- `apps/web/package.json`
- `package-lock.json`
- `.env.example`
- `.env.local`
- `scripts/setup-env.mjs`
- `apps/web/playwright.config.ts`
- `README.md`
- `docs/ai/daily-logs/2026-06-25.md`

### Verification

- `npm install`
- `npm run typecheck`
- `npm run build`
- `npm test`
- `npm run lint`
- Confirmed `@vk/web` dev script now prints `next dev --hostname 127.0.0.1 --port 3456`.
- Confirmed source search has no remaining active `3000` references except migration logic in `scripts/setup-env.mjs`.

### Result

Runtime fixes and the first usable Document Studio are implemented. Word/PPT/Excel drafts can be created from prompt or loaded from supported files (`.docx`, `.pptx`, `.xlsx`, `.csv`, `.tsv`, `.md`, `.txt`), previewed and edited in the browser, exported as preview text, and converted into kanban AI tasks. Figma has a disabled coming-soon modal with prepared contract notes.

### Status

done

### Blockers

- Visual browser QA was not run in this session.
- Excel dependency `xlsx` introduces npm audit warnings; keep under review or replace later with a safer parser/export path.

### Next Steps

- Restart any old dev server and run `npm run dev`; the web UI should open at `http://127.0.0.1:3456`.
- Later upgrade Document Studio export to real `.docx`, `.pptx`, and `.xlsx` generation after deciding the preferred fidelity and template workflow.

---

## Session ‚Äî Direct document preview workflow

### Prompt

User clarified that Document Studio must behave more like opening Word, PowerPoint, or Excel directly in the web UI: selecting/opening a file should immediately show a preview/editor, and document-specific tasks should target the active document for actions such as understanding the assignment first, adding references, editing by prompt, or working on calculations.

### Plan

- Add local orchestrator document reader for supported files so a user can paste a local path and preview it without uploading externally.
- Auto-detect Word/PPT/Excel from file extension instead of requiring the user to select the correct type first.
- Keep local paths visible only in the UI and out of generated kanban task prompts because task data can sync to NocoDB.
- Add document-targeted task actions: understand first, add references, produce output, and edit/refine by prompt.
- Improve preview surfaces to feel closer to Word pages, PowerPoint slides, and Excel sheet tabs/grid.

### Changed Files

- `apps/orchestrator/src/index.ts`
- `apps/web/components/document-studio.tsx`
- `docs/ai/daily-logs/2026-06-25.md`

### Verification

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm test`
- Browser QA at `http://127.0.0.1:3456`:
  - Opened Document Studio modal.
  - Opened the provided DOCX from local path; it rendered as Word preview/editor and task buttons became active.
  - Opened the provided PPTX from local path; it auto-detected PowerPoint and rendered 23 slide preview cards.
  - Confirmed web dev log reports `http://127.0.0.1:3456`.

### Result

Document Studio now supports immediate file preview through file picker or local path for `.docx`, `.pptx`, `.xlsx`, `.csv`, `.tsv`, `.md`, and `.txt`. The active document can be edited in-browser and used to create kanban tasks targeted at document understanding, reference improvement, final output generation, or prompt-based edits.

### Status

done

### Blockers

- Preview is editable and practical but not yet pixel-perfect Office rendering. Exact layout fidelity and exporting back to native `.docx/.pptx/.xlsx` remain future work.

### Next Steps

- Add native export generation for Word/PPT/Excel after deciding whether to prioritize exact templates, editable Office files, or AI-generated drafts first.

---

## Session ‚Äî KarsaDesk rename, native folder picker, and interactive Office workspace

### Prompt

User clarified that the product should feel interactive, like opening Word, Excel, PowerPoint, and eventually Figma; the folder browser should actually help pick local projects across Windows/macOS/Linux; the app should no longer use the existing ‚ÄúVibe Kanban‚Äù name; and README should clearly recommend and explain OpenCode setup.

### Plan

- Rename user-facing product identity to KarsaDesk while preserving compatible internal `VK_` env/table contracts.
- Add a native folder picker endpoint through the local orchestrator with Windows/macOS/Linux strategies and keep the folder tree fallback.
- Make Document Studio feel more like Office: DOCX rendering, PowerPoint slide rail/canvas/notes, and Excel-like formula bar with row/column headers.
- Keep document source paths local and avoid sending them to NocoDB-backed task prompts.
- Rewrite README with OpenCode setup, NocoDB setup, project onboarding, Office workflow, Figma coming soon, and troubleshooting.

### Changed Files

- `package.json`
- `package-lock.json`
- `AGENTS.md`
- `README.md`
- `apps/web/app/layout.tsx`
- `apps/web/app/globals.css`
- `apps/web/components/dashboard.tsx`
- `apps/web/components/dialogs.tsx`
- `apps/web/components/document-studio.tsx`
- `apps/web/components/kanban-board.tsx`
- `apps/web/components/session-workspace.tsx`
- `apps/web/package.json`
- `apps/orchestrator/src/config.ts`
- `apps/orchestrator/src/db.ts`
- `apps/orchestrator/src/git.ts`
- `apps/orchestrator/src/index.ts`

### Verification

- `npm install docx-preview -w @vk/web`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm test`
- Browser QA at `http://127.0.0.1:3456`:
  - Desktop light/default: KarsaDesk heading rendered, local API became `Local service`, Document Studio opened.
  - PowerPoint by prompt: generated preview showed slide rail, `PowerPoint-like editor`, main canvas, and speaker notes.
  - Excel by prompt: generated preview showed cell coordinate/formula bar and editable sheet grid.
  - Project dialog: showed `Browse`, `Go`, root shortcuts including Workspace/Home/Documents, and `Use this folder`.
  - Mobile viewport: onboarding and Add Project stayed accessible; text-only Docs/PPT/Excel label hides as expected behind icon at small width.
  - Dark mode: toggled successfully and `html.dark` was applied.

### Result

KarsaDesk is now the product name in active UI/docs. The app runs on port 3456, has a native-folder-picker path plus local browser fallback, and Document Studio is more interactive for Word/PPT/Excel work. README now gives a clearer OpenCode-first setup path from install/login/provider/model checks to running KarsaDesk.

### Status

done

### Blockers

- Native folder picker cannot be fully automated in QA because it opens an OS dialog; fallback folder tree was verified in-browser.
- Office native export back to `.docx/.pptx/.xlsx` remains future work; current v1 focuses on local preview/edit/task handoff.

### Next Steps

- Add real Office export/save flows when the desired fidelity is decided.
- Later decide the Figma workflow/auth/canvas scope before enabling it beyond coming soon.

---

## Session ‚Äî Add Project responsiveness, OpenCode probe, and Connected Files MVP

### Prompt

User reported Add Project was too slow/stuck, Browse did not open usable folder selection, Use this folder gave no useful feedback, Add repository could stay loading, and OpenCode detection only became correct after refresh/project load. User also asked to continue based on `TASK.md`, which clarifies that Google/Figma should be connected original files rather than an in-app clone editor.

### Audit

- Project dialog used the same busy state for system folder picker and Add repository, so a blocked OS picker could make the whole dialog feel stuck.
- `loadProject` waited for OpenCode provider discovery, making the board feel slow.
- OpenCode was only probed through project-specific endpoint, so first-run/no-project state could show unavailable.
- Document/Figma direction needed to shift toward per-task Connected Files and AI action history.

### Changed Files

- `packages/contracts/src/index.ts`
- `apps/orchestrator/src/schema.ts`
- `apps/orchestrator/src/db.ts`
- `apps/orchestrator/src/index.ts`
- `apps/orchestrator/src/nocodb.ts`
- `apps/web/lib/api.ts`
- `apps/web/components/dialogs.tsx`
- `apps/web/components/dashboard.tsx`
- `apps/web/components/connected-files-panel.tsx`
- `.env.example`
- `README.md`
- `docs/ai/daily-logs/2026-06-25.md`

### Verification

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm test`
- Playwright local QA at `http://127.0.0.1:3456`:
  - OpenCode detected as `OpenCode 1.17.9`.
  - Add Project dialog opened from project rail.
  - `Browse folders` opened the in-modal folder tree without system-dialog blocking.
  - `Use this folder` displayed selected-folder feedback.
  - Created a QA task, opened task inspector, attached a Figma URL in Connected Files, and recorded an AI file action history entry.

### Result

Add Project no longer depends on a potentially blocking OS picker for the primary path. The folder tree is now the primary browse behavior, and system dialog is a secondary option. OpenCode probe is global and project loading no longer waits for provider discovery. Tasks now have Connected Files MVP for Google/Figma original-file links with Open/Ask AI/Detach and action history.

### Status

done

### Blockers

- Google OAuth/Picker and Figma OAuth/API/plugin are scaffolded conceptually but not fully implemented yet.
- The in-app browser plugin was unavailable during QA, so Playwright local fallback was used.

### Next Steps

- Implement Google OAuth + Drive Picker + Docs/Sheets/Slides read/apply adapters.
- Implement Figma PAT/OAuth metadata sync and a future Figma Plugin bridge for selected-frame edits.

---

## Session ó Native Add Project picker and project root fallback

### Prompt

User clarified that Browse/Go in Add Project still did not open a modal or file explorer for choosing a project folder, Add Repository/Use this folder felt unusable before a folder was actually selected, and OpenCode should be visible from the first app load. User also asked to keep the document/Figma direction aligned with `TASK.md`: KarsaDesk should be an AI command center for original Google/Figma files, not a fake full editor.

### Plan

- Make the primary Browse button call the native OS folder picker instead of the in-modal tree.
- Keep an explicit Folder tree fallback for environments where native dialogs cannot open.
- Allow the native picker process to show a window while keeping ordinary CLI/Git processes hidden.
- Start folder-tree browsing from the user's project workspace area instead of the orchestrator package directory.
- Re-check OpenCode first-load status and Add Project modal behavior with local browser QA.

### Changed Files

- `apps/orchestrator/src/process.ts`
- `apps/orchestrator/src/git.ts`
- `apps/web/components/dialogs.tsx`
- `docs/ai/daily-logs/2026-06-25.md`

### Verification

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm test`
- Browser QA at `http://127.0.0.1:3456` using Playwright fallback because the in-app browser surface was unavailable:
  - Desktop/default: OpenCode rendered on first load as `OpenCode 1.17.9`.
  - Desktop/default: Add Project dialog opened from the project rail and showed `BrowseÖ`, `Folder tree`, and `Go to path`.
  - Desktop/default: Folder tree fallback opened at `C:\Users\mnur2\Documents\Programming\Work` and `Use this folder` populated Repository path.
  - Mobile viewport: Add Project dialog still exposed Browse/Folder tree/Go controls without console errors.
  - Desktop dark mode: Add Project dialog controls remained visible without console errors.

### Result

The Add Project flow now has an unambiguous primary `BrowseÖ` button for the native OS folder picker, plus a separate `Folder tree` fallback. Windows native picker is launched without `windowsHide`, so the folder dialog is allowed to appear. Folder tree default location now targets the user's project workspace area instead of `apps/orchestrator`, making project selection much less confusing.

### Status

done

### Blockers

- Native OS folder dialogs cannot be clicked safely from headless QA because they can block automation; the code path was fixed and the in-modal fallback was verified.

### Next Steps

- If the native Windows dialog still does not appear in a visible local terminal session, replace `FolderBrowserDialog` with a Shell COM picker or Electron/Tauri native dialog bridge.
- Continue Google/Figma connector work from `TASK.md`: OAuth/Picker/API adapters and action history against original files.

---

## Session ó Google/Figma connected-file agent MVP

### Prompt

User asked to implement `TASK.md` clearly: KarsaDesk must become an AI command center/agent for original Google Docs, Google Sheets, Google Slides, and Figma files. Users should login/connect Google/Figma, attach real files to kanban tasks, open/edit files in the original apps, ask AI from task context, and keep action history. User emphasized this is like coding diff/review, but for Google Docs/Sheets/Slides/Figma, not an in-app fake editor.

### Plan

- Audit existing Connected Files MVP and keep the product direction from `TASK.md`: no fake Word/Figma editor and no iframe-as-main solution.
- Add local connected-account storage for Google/Figma with encrypted tokens and public connection status.
- Add Google OAuth start/callback, Figma OAuth start/callback, and Figma PAT connect for development.
- Add Google Drive listing, Google metadata sync/export context, Figma metadata sync/tree summary, and per-file AI preview/action history.
- Update task inspector UI so Connected Files can connect providers, search Google files, attach provider files, sync metadata, and prepare AI previews.
- Fix mobile inspector access so Connected Files is usable on small screens.
- Update env/docs and verify build/lint/test/browser QA.

### Changed Files

- `packages/contracts/src/index.ts`
- `apps/orchestrator/src/schema.ts`
- `apps/orchestrator/src/db.ts`
- `apps/orchestrator/src/config.ts`
- `apps/orchestrator/src/connected-providers.ts`
- `apps/orchestrator/src/index.ts`
- `apps/orchestrator/src/nocodb.ts`
- `apps/web/components/connected-files-panel.tsx`
- `apps/web/components/dashboard.tsx`
- `.env.example`
- `README.md`
- `docs/ai/daily-logs/2026-06-25.md`

### Verification

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm test`
- Browser QA at `http://127.0.0.1:3456` using Playwright fallback because the in-app browser surface was unavailable:
  - Desktop/default: OpenCode rendered on first load as `OpenCode 1.17.9`.
  - Desktop/default: Connected Files panel rendered provider status, Google/Figma connect controls, Google Drive picker, Figma PAT, and AI preview button.
  - Desktop/dark: Connected Files panel rendered Google Drive picker, Figma PAT, and AI preview button without console errors.
  - Mobile: task inspector now opens as a slide-over; Connected Files, Google Drive picker, Figma PAT, and AI preview button are visible without console errors.
  - Temporary QA task was removed from local SQLite after QA.

### Result

KarsaDesk now has a real connector foundation for Google/Figma: OAuth/PAT endpoints, encrypted local token storage, public account status, Google Drive file listing, Google/Figma metadata sync, provider-file attach, context reading, and AI action history with safe preview/needs-confirmation semantics. The UI now communicates that KarsaDesk works against original files and does not create a fake embedded editor.

### Status

done

### Blockers

- Real provider login was not completed during QA because no Google/Figma OAuth credentials or PAT were provided in this run.
- Direct write/apply into Google Docs/Sheets/Slides and Figma canvas remains intentionally gated behind future apply adapters and Figma Plugin bridge to avoid destructive edits without explicit confirmation.

### Next Steps

- Add Google Picker UI script integration once OAuth client is configured.
- Implement explicit Apply endpoints for small Docs/Sheets/Slides changes with before/after preview.
- Add Figma Plugin bridge so selected-frame edits can be applied inside the real Figma editor.
