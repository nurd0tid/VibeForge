# 2026-06-26

## Smart Prompt fallback and AI chat panel

- Prompt: Fix Smart Prompt requests that waited too long and ended with 500, then add a kanban-side AI chat/brainstorming area for testing ideas before turning them into tasks.
- Plan:
  - Add a bounded AI request timeout for OpenCode-backed plan/chat calls.
  - Return a structured local fallback for Smart Prompt when OpenCode is unavailable, unselected, fails, or times out.
  - Add a project-scoped AI chat endpoint with fallback responses.
  - Add an AI chat panel in the dashboard with provider/model selection, local fallback default, copy, send, and "Smart" handoff into Smart Prompt.
  - Let Smart Prompt run in local fallback mode without requiring provider/model selection.
- Changed files:
  - `.env.example`
  - `apps/orchestrator/src/config.ts`
  - `apps/orchestrator/src/index.ts`
  - `apps/orchestrator/src/opencode.ts`
  - `apps/orchestrator/src/ai-fallback.ts`
  - `apps/web/components/ai-brainstorm-panel.tsx`
  - `apps/web/components/dashboard.tsx`
  - `apps/web/components/dialogs.tsx`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
  - Manual endpoint QA: `/api/projects/:projectUid/ai-chat` without provider/model returned fallback in ~23ms.
  - Manual endpoint QA: `/api/projects/:projectUid/smart-prompt` without provider/model returned fallback draft in ~10ms.
  - Browser QA via local Chrome because in-app browser was unavailable:
    - Desktop: AI chat visible, fallback option visible, Send works, fallback badge appears, Smart opens Smart Prompt with generated text.
    - Mobile: AI chat visible, fallback option visible, Send visible.
- Result:
  - Smart Prompt no longer has to fail with 500 when the AI provider is slow or unavailable.
  - AI chat is now available inside the kanban as a fast brainstorming/test area.
  - Local fallback is the default for chat and Smart Prompt until the user intentionally selects an OpenCode provider/model.
- Blockers / next steps:
  - The UI QA reported one non-blocking 404 resource load, likely a favicon/dev asset. Core app behavior passed.

## Smart Prompt agent thinking console

- Prompt: Smart Prompt should show a thinking/status experience like using an AI CLI instead of only showing a silent spinner.
- Plan:
  - Add a terminal-like Smart Prompt status console.
  - Show safe operational thinking/status steps: prompt received, project context loaded, provider/planner selected, planner running, and draft normalization.
  - Keep hidden model reasoning private; display task progress and agent state only.
  - Make local fallback mode and empty provider/model payloads work cleanly.
- Changed files:
  - `apps/web/components/dialogs.tsx`
  - `apps/orchestrator/src/index.ts`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
  - Desktop Chrome QA: Smart Prompt displays Agent thinking, elapsed status, prompt/context/provider/planner/normalize log, and produces a publishable draft.
  - Mobile Chrome QA: Smart Prompt opened through AI chat Smart button, Agent thinking appears, normalize step appears, and publish action is available.
- Result:
  - Smart Prompt now feels closer to a supervised CLI agent run, with visible progress while the draft is being generated.
  - Empty `providerId`/`modelId` values no longer break fallback generation.
- Blockers / next steps:
  - Browser QA still reports one non-blocking 404 resource load, likely favicon/dev asset related.

## Running workspace clarity, Smart Prompt tuning, and localhost OAuth reset

- Prompt: User reported the running session UI was hard to read, thinking timeline did not feel live/auto-scrolled, session paused/failed state was unclear, retry/resume controls were missing, Smart Prompt needed per-user/per-workflow tuning, Figma OAuth failed with invalid scopes, callbacks should return to `localhost:3456`, and old task/session data should be cleared.
- Plan:
  - Redesign the Agent tab around clear state cards, a context/compact meter, a live thinking/search/tool timeline, and a readable queue panel.
  - Add retry/resume actions for paused/failed/review queues.
  - Auto-create one default session per project after OpenCode provider/model discovery, then reuse it for queued tasks.
  - Add Smart Prompt profiles for coding, Google Workspace docs/slides/sheets, Figma, and general assistant work, plus custom tuning text.
  - Make Figma OAuth scopes configurable and default to minimal `current_user:read`.
  - Revert local callback/env defaults to `http://localhost:3456`.
  - Reset local runtime task/session/event/review/daily log/outbox data while preserving projects and Google login.
- Changed files:
  - `.env.example`
  - `apps/orchestrator/src/config.ts`
  - `apps/orchestrator/src/connected-providers.ts`
  - `apps/orchestrator/src/index.ts`
  - `apps/web/components/dashboard.tsx`
  - `apps/web/components/dialogs.tsx`
  - `apps/web/components/session-workspace.tsx`
  - `apps/web/next.config.ts`
  - `.env.local` (local only, not committed)
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
  - Secret scan of tracked diff found no real OAuth/PAT secrets.
- Result:
  - Running workspace now shows effective state such as paused-after-failure, latest error detail, active task, approval count, diff count, estimated context usage, compact recommendation, provider/model, and auto-scrolling tool timeline.
  - Queue cards now make running/review/done/failed states visually distinct and surface retry availability.
  - Smart Prompt can be tuned per workflow before generating tasks.
  - Figma connect uses configurable minimal scopes by default.
  - Local runtime task/session data was cleared; one old managed worktree folder remained locked by another process and needs that process closed before deletion.
- Blockers / next steps:
  - Remote NocoDB destructive cleanup was not performed automatically to avoid deleting shared remote records unexpectedly; future task/session writes are still enqueued and now trigger immediate sync attempts.
  - Reconnect Figma after restarting the app so the new scope applies.

## Run task/session UX and local provider env setup

- Prompt: User could not understand how to run existing tasks, sessions were not obvious, and Google/Figma OAuth values needed to be placed in local env.
- Plan:
  - Keep provider credentials local-only in `.env.local` and do not document or commit the values.
  - Make task running work even when no session has been selected yet by auto-creating a supervised default session from the first detected OpenCode provider/model.
  - Add an inspector-level `Run this task` action.
  - Keep `Selected` explicit but let `Next` and `Run all` create/select a session automatically.
  - Let the run API queue Backlog/Ready/Failed tasks and normalize queued tasks to Ready.
  - Reject unsafe queue states such as already running, waiting approval, or done.
- Changed files:
  - `.env.local` (local-only, not committed)
  - `apps/web/components/dashboard.tsx`
  - `apps/orchestrator/src/index.ts`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
- Result:
  - Users can run one task from the inspector with `Run this task`.
  - `Next` and `Run all` can start work without a preselected session when OpenCode providers are detected.
  - The backend now queues Backlog/Ready/Failed tasks, so newly created Backlog tasks are no longer stuck before execution.
  - `.env.local` now has the requested Google/Figma OAuth values locally without adding them to tracked source.
- Blockers / next steps:
  - OAuth client secrets should still be rotated later because they were shared in chat; keep them out of Git and logs.

## Domain callback env and footer AI/model picker

- Prompt: User wants local env to use `karsa-desk.com:3456` instead of `127.0.0.1` for Google/Figma OAuth callbacks, and wants to choose default provider/model from the main run controls before testing tasks.
- Plan:
  - Update local-only `.env.local` to use the domain callback and browser-facing orchestrator URL without committing secrets.
  - Add a Next route handler callback proxy on the web app so Google/Figma can redirect to port 3456 and still complete the local orchestrator OAuth flow.
  - Make the web dev/start script read `VK_WEB_HOST` and `VK_WEB_PORT` from env instead of hardcoding loopback.
  - Add provider/model dropdowns to the board footer; changing them clears the selected existing session so the next run creates a quick session with that provider/model.
  - Verify build and type safety.
- Changed files:
  - `.env.local` (local-only, not committed)
  - `apps/web/app/api/connect/[provider]/callback/route.ts`
  - `apps/web/components/dashboard.tsx`
  - `apps/web/package.json`
  - `scripts/next-web.mjs`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
- Result:
  - OAuth redirect can now target `http://karsa-desk.com:3456/api/connect/google/callback` and `http://karsa-desk.com:3456/api/connect/figma/callback`.
  - Footer now shows AI provider and model selectors used by quick sessions.
  - `npm run dev`/`start` now reads the host/port from env.
- Blockers / next steps:
  - Windows hosts file currently maps `karsa-desk.com` to `172.0.0.1`; updating it to `127.0.0.1 karsa-desk.com` requires Administrator permissions and was denied from this process.

## Windows Next dev launcher fix

- Prompt: `npm run dev` failed on Windows with `spawn EINVAL` from `scripts/next-web.mjs`.
- Plan:
  - Replace the Windows-sensitive `npx` spawn with a direct Node launch of Next's local CLI.
  - Convert filesystem URL cwd usage to a normal Windows path string.
  - Add `VK_WEB_BIND_HOST` documentation so public callback URL and internal bind address can be configured separately.
  - Verify that dev server starts and that the remaining domain issue is DNS/hosts, not the script.
- Changed files:
  - `scripts/next-web.mjs`
  - `.env.example`
  - `.env.local` (local-only, not committed)
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run dev` starts Next and orchestrator without `spawn EINVAL`.
  - `Invoke-WebRequest http://localhost:3456` returns `200`.
  - `Invoke-WebRequest http://karsa-desk.com:3456` still times out because Windows resolves the domain to `172.0.0.1`.
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
- Result:
  - The dev launcher is fixed for Windows.
  - KarsaDesk can start locally again.
- Blockers / next steps:
  - Edit `C:\Windows\System32\drivers\etc\hosts` as Administrator and set `127.0.0.1 karsa-desk.com`; current `172.0.0.1` mapping prevents the custom domain from reaching the local server.

## Allow karsa-desk.com dev origin

- Prompt: Next.js dev server blocked cross-origin dev resources such as `/_next/webpack-hmr` when the app was opened from `karsa-desk.com`.
- Plan:
  - Read the local Next.js 16.2.6 `allowedDevOrigins` documentation.
  - Add `karsa-desk.com` to `apps/web/next.config.ts`.
  - Verify typecheck, lint, tests, and production build.
- Changed files:
  - `apps/web/next.config.ts`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
- Result:
  - Next dev HMR/resources are allowed for `karsa-desk.com` after restarting `npm run dev`.
- Blockers / next steps:
  - Restart the dev server for the config change to take effect.

## Task run console and kanban run lanes

- Prompt: Task execution should also show diff/thinking/timeline like using a CLI and like modern AI kanban tools such as SumoPod, Vibe Kanban, and Cline Kanban.
- Plan:
  - Publish explicit backend run markers around prompt preparation, OpenCode execution, response capture, checkpoint creation, and review readiness.
  - Upgrade the Session Workspace Agent tab into a CLI-like run console with status cards, run timeline, approval state, active task, and diff hash/changed-file summary.
  - Make session queue task cards clickable and show the latest task event.
  - Add board-level run lanes for running, waiting approval, review/diff-ready, done, failed, and assigned-session states.
- Changed files:
  - `apps/orchestrator/src/index.ts`
  - `apps/web/components/session-workspace.tsx`
  - `apps/web/components/kanban-board.tsx`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
  - Desktop Chrome QA: app boots, board renders, AI chat/session controls render, and no app runtime crash was observed.
- Result:
  - Task/session execution now has visible operational thinking/status markers instead of only passive status changes.
  - The Agent tab is closer to a CLI run view: timeline, active task, approvals, diff state, event cards, and per-task queue context.
  - Kanban task cards now surface execution lifecycle context directly on the board.
- Blockers / next steps:
  - Local data currently has 0 sessions and 0 tasks, so live run-lane visual QA could not be exercised without mutating the user's local DB.
  - Browser QA still reports one non-blocking 404 resource load, likely favicon/dev asset related.

## AI chat provider error and loading state

- Prompt: AI chat selected OpenCode provider showed `No response` even though the CLI reported `Insufficient balance`; it should show loading and provider errors like a terminal.
- Plan:
  - Treat empty OpenCode chat responses as provider failures instead of successful blank replies.
  - Capture OpenCode event diagnostics for billing, quota, unauthorized, invalid API key, and similar provider errors.
  - Redact obvious API-key shaped secrets from diagnostics.
  - Show a pending assistant bubble while chat requests are running.
  - Replace pending with fallback/error content instead of rendering `No response`.
- Changed files:
  - `apps/orchestrator/src/opencode.ts`
  - `apps/orchestrator/src/opencode.test.ts`
  - `apps/orchestrator/src/ai-fallback.ts`
  - `apps/web/components/ai-brainstorm-panel.tsx`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
  - Unit test: provider diagnostics extract insufficient-balance messages and redact obvious API keys.
  - Desktop Chrome QA: local fallback chat returns a real fallback message and no longer shows `No response`.
  - Desktop Chrome QA with delayed network intercept: pending/loading bubble appears and is replaced by the final response.
- Result:
  - Provider failures such as insufficient balance or invalid API key are now surfaced as actionable fallback/error messages.
  - AI chat now visibly loads while waiting for OpenCode/local fallback.
- Blockers / next steps:
  - I did not intentionally call a paid remote provider during QA to avoid extra model/billing usage.

## Smart Prompt tmp-kanban format and live Figma docs

- Prompt: Smart Prompt output was too poor; it should follow the user's `b2b-template/docs/ai/tmp-kanban-prompts` style, respect AGENTS/AI memory/daily logs, split only when needed, improve UI preview, and README must fully explain Google Workspace/Figma setup with Figma live.
- Plan:
  - Read tmp-kanban examples from `b2b-template`.
  - Rewrite Smart Prompt local fallback to produce ready-to-run tmp-kanban style prompts with sections for request, tujuan, kerjakan, acceptance criteria, verification, frontend demo when relevant, and daily log.
  - Update the OpenCode planning template to request the same tmp-kanban structure from remote AI.
  - Include existing KarsaDesk task count and `docs/ai/tmp-kanban-prompts` file list in Smart Prompt context.
  - Improve Smart Prompt draft preview UI with markdown preview, acceptance/verification panels, raw markdown editor, and wider modal.
  - Replace Figma coming-soon modal with live Figma setup/connect modal.
  - Expand README with Google Cloud OAuth, Google Workspace workflow, Figma OAuth/PAT, Figma live workflow, connected-file behavior, and troubleshooting.
- Changed files:
  - `README.md`
  - `apps/orchestrator/src/ai-fallback.ts`
  - `apps/orchestrator/src/ai-fallback.test.ts`
  - `apps/orchestrator/src/db.ts`
  - `apps/orchestrator/src/index.ts`
  - `apps/web/components/dialogs.tsx`
  - `apps/web/components/dashboard.tsx`
  - `apps/web/components/figma-live-modal.tsx`
  - `apps/web/components/figma-coming-soon.tsx`
  - `docs/ai/daily-logs/2026-06-26.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
  - Desktop Chrome QA: Figma header opens Figma Live modal with OAuth, PAT, Open Figma, and no "coming soon" text.
  - Unit test: coherent Smart Prompt request stays one task; explicit numbered list splits into dependency-aware cards.
- Result:
  - Smart Prompt fallback now follows the tmp-kanban prompt style instead of shallow sentence splitting.
  - Figma is documented and surfaced as live setup/connect workflow.
  - README now documents Google Workspace and Figma setup end-to-end.
- Blockers / next steps:
  - Browser QA still reports one non-blocking 404 resource load, likely favicon/dev asset related.
