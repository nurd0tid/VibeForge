# Daily Log — 2026-06-27

## Google/Figma file selection, session controls, and NocoDB sync-all

- Prompt: User wants running sessions deletable after pause, session AI/provider/model choices to persist and be editable before retry, Google Drive files to appear by Docs/Sheets/Slides and be selectable for prompt/AI work instead of only opening the original file, Figma next steps to be clear after PAT/OAuth connect, AI chat closed by default, responsive modals, and structured backlog/done/session data pushed to NocoDB rather than feeling local-only.
- Plan:
  - Add Google Drive readonly scope and server-side file-type filtering for Docs/Sheets/Slides.
  - Let Google Workspace modal select a file, attach it to the currently selected kanban task, and run a preview AI file action from a prompt.
  - Let Figma modal attach the pasted Figma file to the selected task and run a preview AI file action.
  - Make Google/Figma modals scroll within viewport and avoid overflow on smaller screens.
  - Persist default provider/model per project in localStorage and keep AI chat closed until opened.
  - Add session provider/model selectors inside the running session workspace for pause/change/retry workflows.
  - Add session deletion that stops local runtime, removes the managed Git worktree, clears local session console artifacts, and leaves tasks on the board.
  - Add NocoDB sync-all endpoint and make NocoDB setup button also push all current structured local records.
- Changed files:
  - `apps/orchestrator/src/connected-providers.ts`
  - `apps/orchestrator/src/db.ts`
  - `apps/orchestrator/src/git.ts`
  - `apps/orchestrator/src/index.ts`
  - `apps/web/components/dashboard.tsx`
  - `apps/web/components/figma-live-modal.tsx`
  - `apps/web/components/google-workspace-modal.tsx`
  - `apps/web/components/session-workspace.tsx`
  - `docs/ai/daily-logs/2026-06-27.md`
- Verification:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
- Result:
  - Google Drive file picker can filter by the selected Docs/Sheets/Slides tab and needs one Google reconnect to gain `drive.readonly` for older Drive files.
  - Google/Figma can now attach selected external files to the selected task and produce an AI preview/action from a prompt.
  - Session workspace now supports provider/model switching while paused/review/idle, retry with the changed model, and deleting local session runtime/worktree.
  - AI chat is no longer open by default.
  - NocoDB setup now also attempts full structured sync of projects, tasks, sessions, and daily logs.
- Blockers / next steps:
  - Local SQLite remains necessary as runtime cache/outbox for a local-first app; raw terminal logs, worktree paths, events, and diffs intentionally remain local and are not sent to NocoDB.
